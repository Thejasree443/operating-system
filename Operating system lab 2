echo "# operating-system" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Thejasree443/operating-system.git
git push -u origin main
fclose(fptr1); 

    fclose(fptr2); 

    return 0; 
}
